# Beagle Triage Report & Implementation Proposal

- [ ] **Ready to execute**: Check this box to start autonomous implementation

> Triaged from Issues: #24

### Deduplication & Noise Filter

Prior to clustering, the inbox was evaluated for noise, accidental submissions, and duplicates:

> [!NOTE]
> **Issue #24** is explicitly flagged with `(test-issue-from-agent)`. While generated synthetically to test the feedback pipeline, the core request is a highly constructive accessibility and user-experience improvement (adding a `title` attribute to an interactive icon button). It has been promoted to an active task rather than discarded.

---

### Clustered Proposals

#### UI & Dashboard Display
* **Floating Feedback Button Accessibility Enhancement (#24)**: Users hover over the floating feedback button (the microphone icon) but lack instant visual text tooltips. Adding a `title` attribute solves this for mouse users and aids screen readers.

---

### Proposed Action Items

#### 1. Implement Tooltip on Floating Feedback Button
* **Target File**: `FloatingFeedbackButton.tsx` (typically located in `/src/components/` or `/components/feedback/`)
* **Technical Changes**:
  * Locate the root `<button>` element (or the wrapper component) rendered inside the `FloatingFeedbackButton` component.
  * Append the `title` attribute to the button element:
    ```tsx
    title="Talk to the Beagle"
    ```
  * Ensure appropriate `aria-label` attributes are also present or matching to maintain strong accessibility compliance.
* **Verification Steps**:
  1. Spin up the Scouty Planner local development server.
  2. Hover your cursor over the floating microphone button in the bottom-right corner of the viewport.
  3. Verify that a native browser tooltip reading **"Talk to the Beagle"** appears after a brief delay.
  4. Inspect the DOM to confirm the `<button>` node contains `title="Talk to the Beagle"`.

---
