# Beagle Triage Report & Implementation Proposal

- [x] **Ready to execute**: Auto-approved for 0-touch execution (Low Risk)

> Triaged from Issues: #32, #28

# Implementation Proposal: Scouty Planner Updates

## 1. Clustered Proposals

### Calendar & Activities

#### Integration of Aria's Soccer Calendar (Issue #28)
*   **User Request:** Add the OSFC Girls U13 soccer calendar under Aria's profile.
*   **Source Provided:** `https://calendar.google.com/calendar/embed?src=dno68vgi29lj52e9rg29ubn2dv3i3bri%40import.calendar.google.com&ctz=America%2FNew_York`
*   **Proposed Action:** Extract the calendar ID from the embed link and translate it into a direct iCal/ICS subscription feed for parsing. Integrate this feed with Aria's active calendar listings on the dashboard.

---

### Child Avatars & Icons

#### Custom Badge/Emoji Override for Brighton (Issue #32)
*   **User Request:** Add a poop emoji (`💩`) badge to Brighton’s display name.
*   **Context:** Submitted by "Dad" (Andrew/Chris) as a playful/agentic test. 
*   **Proposed Action:** Update the child metadata configuration to support custom badge overrides or nickname badges, allowing the requested emoji to be conditionally displayed next to Brighton's name on the dashboard without hardcoding logic directly into core UI components.

---

## 2. Proposed Action Items

### Task A: Add Aria's OSFC Girls U13 Soccer Calendar
*   **Files to Inspect/Modify:**
    *   `src/config/calendars.json` or `src/config/children.ts` (where child-to-calendar mappings are defined).
    *   `src/services/calendarService.ts` (the service processing external ICS subscriptions).
*   **Implementation Steps:**
    1. Parse the calendar ID `dno68vgi29lj52e9rg29ubn2dv3i3bri@import.calendar.google.com` from the provided embed URL.
    2. Construct the direct public iCal feed URL:  
       `https://calendar.google.com/calendar/ical/dno68vgi29lj52e9rg29ubn2dv3i3bri%40import.calendar.google.com/public/basic.ics`
    3. Add a new calendar subscription entry for **Aria** with the label `OSFC Girls U13 Soccer` using this direct iCal URL.
    4. Ensure the dashboard's calendar sync scheduler picks up this new feed on its next execution.
*   **Verification Steps:**
    1. Verify that a GET request to the public ICS URL returns an HTTP 200 and a valid `text/calendar` response containing VEVENT items.
    2. Run local integration tests to verify the parser correctly associates these events with Aria's profile.
    3. Verify that soccer practice/game events appear on the family planner dashboard under Aria's color theme.

> [!IMPORTANT]  
> **Resource Verification:** The derived feed URL `https://calendar.google.com/calendar/ical/dno68vgi29lj52e9rg29ubn2dv3i3bri%40import.calendar.google.com/public/basic.ics` must be validated for reachability and correct schema structure (RFC 5545) before merging.

---

### Task B: Support Custom Child Badges/Emojis (Brighton's Request)
*   **Files to Inspect/Modify:**
    *   `src/config/children.ts` (metadata configuration for family children).
    *   `src/components/ChildAvatar.tsx` or `src/components/ChildNameTag.tsx` (the UI components rendering names/avatars).
*   **Implementation Steps:**
    1. Add an optional `badge` property (string type for emojis or icon keys) to the child configuration interface.
    2. In the configuration file, locate **Brighton** and add `"badge": "💩"`.
    3. Modify the rendering component to check for the presence of a `badge` property and render it adjacent to the child's display name/avatar.
*   **Verification Steps:**
    1. Run the local development server and navigate to the dashboard route.
    2. Verify that Brighton's display name now features the `💩` badge cleanly in a badge container.
    3. Ensure no visual regressions or layout shifts occur in mobile viewport layouts (`430x775`).

---

## Risk Assessment

RISK_TIER: LOW - The tasks involve adding a standard external Google Calendar subscription feed and updating a frontend UI metadata property for child avatars.

---

## Noise & Audio Tests
